<% content_for :title, "Edit Pick Random Settings" %>

<div class="p-4 w-full max-w-6xl mx-auto">
  <div class="mb-6">
    <h1 class="font-bold text-4xl">Pick Random Settings — <%= @media_type %></h1>
    <p class="themed-page-subtitle mt-1">Configure which <%= @media_type %> items qualify as random candidates.</p>
  </div>

  <%= render "form", config: @config, media_type: @media_type %>
</div>
