<% content_for :title, "Edit Site Settings" %>

<div class="p-4 w-full max-w-6xl mx-auto">
  <div class="mb-6">
    <h1 class="font-bold text-4xl">Edit Site Settings</h1>
    <p class="themed-page-subtitle mt-1">Customize the header banner shown across the site.</p>
  </div>

  <%= render "form", site_setting: @site_setting %>
</div>
